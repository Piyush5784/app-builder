# name: Deploy Server

# on:
#   push:
#     branches: [main]
#   workflow_dispatch: {}

# concurrency:
#   group: deploy-server
#   cancel-in-progress: false

# jobs:
#   deploy:
#     name: Deploy apps/server to AWS
#     runs-on: ubuntu-latest
#     environment: production

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Set up Bun
#         uses: oven-sh/setup-bun@v2
#         with:
#           bun-version: "1.3.14"

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ vars.AWS_REGION }}

#       - name: Install
#         run: bun install --frozen-lockfile

#       - name: Deploy
#         run: bunx sst deploy --stage production
#         env:
#           FRONTEND_URL: ${{ vars.FRONTEND_URL }}
#           BETTER_AUTH_URL: ${{ vars.BETTER_AUTH_URL }}
#           BETTER_AUTH_SECRET: ${{ secrets.BETTER_AUTH_SECRET }}

#           # Should be Neon's *pooled* connection string for this deploy —
#           # see sst.config.ts's comment on DATABASE_URL.
#           DATABASE_URL: ${{ secrets.DATABASE_URL }}

#           UPSTASH_REDIS_REST_URL: ${{ secrets.UPSTASH_REDIS_REST_URL }}
#           UPSTASH_REDIS_REST_TOKEN: ${{ secrets.UPSTASH_REDIS_REST_TOKEN }}

#           GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
#           GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
#           GH_CLIENT_ID: ${{ secrets.GH_CLIENT_ID }}
#           GH_CLIENT_SECRET: ${{ secrets.GH_CLIENT_SECRET }}

#           RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}

#           GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
#           NVIDIA_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
#           GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
#           OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
#           ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

#           E2B_API_KEY: ${{ secrets.E2B_API_KEY }}
#           E2B_TEMPLATE_ID: ${{ vars.E2B_TEMPLATE_ID }}
#           SANDBOX_TIMEOUT_MS: ${{ vars.SANDBOX_TIMEOUT_MS }}
